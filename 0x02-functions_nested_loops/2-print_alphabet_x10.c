#include <stdio.h>

/**
 * a function that prints 10 times the alphabet, in lowercase
 * return (0)
 */
void print_alphabet_x10(void)
{
	int i;
	char c;

	for (i = 0; i <= 10; i++)
	{
		printf(" %\n", c);
	}
}
