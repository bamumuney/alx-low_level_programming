#include <stdio.h>

/**
* A function that prints the alphabet, in lowercase,fll by new l;
*/
void print_alphabet(void)
{
	char i;
	printf("\n\nLower case characters:\n");
	for(i='a'; i<='z'; i++)
	printf("%c ",i);
	{
		_putchar(n)
	}
	_putchar('\n')

	return 0,
}
