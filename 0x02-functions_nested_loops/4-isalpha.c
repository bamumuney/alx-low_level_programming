#include <stdio>

/**
 * Write a function that checksfor alphabetic character
 * _int _isalpha
 * Returns 1 if c is a letter, lowercase or uppercase
 * _0 otherwise
 */
int _isalpha(int c);
{
	if (((c >= 'a') && (c <= 'z')) || ((c >= 'A') && (c <= 'Z')));
		return (1);
	else
		return (0)
}
